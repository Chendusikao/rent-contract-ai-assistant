# ========== 生产环境配置 ==========

# DeepSeek API Key（必填）
DEEPSEEK_API_KEY=sk-your-deepseek-api-key

# ========== 前后端分离部署配置 ==========
# 场景一：后端同进程托管前端（简单部署）
#   不需要额外配置，npm run build 后 npm run server 即可
#   前端会请求同源 /api

# 场景二：前端独立部署（Nginx/CDN/对象存储）
#   构建前端时设置 VITE_API_BASE_URL 指向后端域名：
#   VITE_API_BASE_URL=https://api.example.com
#
#   然后在部署前端静态文件到 Nginx 时，确保 /api 请求转发到后端：
#   location /api {
#     proxy_pass http://127.0.0.1:3001;
#   }
